export declare type StatemachineDefinition<States extends string> = {
    initial: States;
    states: {
        [State in States]: Array<States>;
    };
};
export declare type Statemachine<States extends string> = {
    current: States;
    reset: () => void;
} & {
    [State in States]: <T extends () => any>(entry?: T, exit?: () => void) => T extends () => infer U ? U : void;
};
export declare function statemachine<States extends string>(definition: StatemachineDefinition<States>): Statemachine<States>;
